import { ProductView } from "@/templates/ProductView"

interface ProductRouteProps{

}

const ProductRoute = ({}: ProductRouteProps) => {
    return (
        <ProductView />
    )
}

export default ProductRoute